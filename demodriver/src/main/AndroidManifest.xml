<?xml version="1.0" encoding="utf-8"?>
<!--
  No activity, no service, no permissions: this app is a host for instrumentation and never runs
  anything of its own. See build.gradle.kts for why it is a separate app at all.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:label="Demo driver" />
</manifest>
